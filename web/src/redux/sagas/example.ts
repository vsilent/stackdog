// import { put, call, takeEvery } from 'redux-saga/effects';

// function* exampleHook() {
//     const links = yield call(actionCall);
//     yield put(links);
// }

// Initializer
export default function* exampleSaga() {
    // yield takeEvery(actionEvent, exampleHook);
}